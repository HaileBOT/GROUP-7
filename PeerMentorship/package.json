{
  "name": "peer-mentorship-platform",
  "version": "1.0.0",
  "description": "Peer Mentorship and Real-Time Academic Support Platform",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm.cmd run server\" \"npm.cmd run client\"",
    "server": "cd backend && npm.cmd run dev",
    "client": "cd frontend && npm.cmd start",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  },
  "dependencies": {
    "axios": "^1.13.2"
  }
}
